---
import RedesignLayout from '../../layouts/RedesignLayout.astro';
import PageHero from '../../components/band/PageHero.astro';
import RdFeature from '../../components/redesign/RdFeature.astro';
import LeafHeading from '../../components/redesign/LeafHeading.astro';
import Leaf from '../../components/redesign/Leaf.astro';
import RdCta from '../../components/redesign/RdCta.astro';
import ExpenseSplit from '../../components/redesign/ExpenseSplit.astro';
import directorsData from '../../data/directors.json';

// Leadership shown on the Team page: founder + U.S. Executive Directors + Africa Country Director.
// Full board lives on /about/board/. Source: Webflow CMS Directors collection.
const leadershipSlugs = ['carole-ward', 'stephanie-maier', 'terry-lynne-goodman'];
const leadership = leadershipSlugs
  .map(slug => directorsData.find(d => d.slug === slug))
  .filter(Boolean);
const personHref = (slug) => slug === 'carole-ward' ? '/about/our-story/founder/' : `/about/people/${slug}/`;

// The full U.S. roster, per Will (2026-07-07). Source: Favor Staff Directory
// 2026 (Telephone List). Names + titles only; the directory's phones, home
// addresses, and birthdays are private and must never be published. The three
// directors above are excluded here since they are featured already.
const roster = [
  { name: 'Bradley Cutsinger', title: 'Regional Development Director' },
  { name: 'Brian Carr', title: 'Regional Development Director' },
  { name: 'Celeste Paul', title: 'Volunteer' },
  { name: 'Crystal Hall', title: 'Grants Management Specialist' },
  { name: 'Crystal Moux', title: 'Administrative Assistant' },
  // Daniel Casella removed at his own request (2026-08-05, security).
  { name: 'Danny Avila', title: 'Social Media Manager' },
  { name: 'David Morton', title: 'Sr. Regional Development Director' },
  { name: 'Emma Stewart', title: 'Administrative Assistant (Marketing/Support Team)' },
  { name: 'Grace Morris', title: 'Executive Administrative Assistant' },
  { name: 'Gregory Phipps', title: 'Grants Specialist' },
  { name: 'Jennifer Morris', title: 'Partner Care Manager' },
  { name: 'Jerry Christian', title: 'Volunteer' },
  { name: 'Joe Krol', title: 'Assistant Director of Grant Services' },
  { name: 'Jordan Abbott', title: 'Administrative Assistant (Grant Dept.)' },
  { name: 'Josh Milliron', title: 'National Director of Church Engagement' },
  { name: 'Megan Respass', title: 'Direct Marketing Associate' },
  { name: 'Michael Hinton', title: 'Operations Manager' },
  { name: 'Morgan Mason', title: 'Executive Administrative Assistant' },
  { name: 'Nathan Bril', title: 'Director of Media' },
  { name: 'Rachel Cox', title: 'Executive Assistant' },
  { name: 'Richard Brown', title: 'Regional Development Director' },
  { name: 'Stephanie Brady', title: 'Regional Development Director' },
  // Val Spillane removed 2026-08-05: no longer with Favor.
  { name: 'William Hamilton', title: 'Marketing Consultant' },
];
---
<RedesignLayout
  title="Our Team"
  description="The U.S. executive team and Africa country leadership of Favor International."
>
  <PageHero
    label="Our Team"
    title="A small U.S. team serving a massive field."
    promise="An army of indigenous missionaries carries the work across Uganda, South Sudan, Chad, and nations beyond. Our U.S. team exists to serve them, and we would not have it any other way."
    image="/images/field-2026/evangelism-5.webp"
    imageAlt="Favor leadership at a training session"
  />

  <!-- Order set by Stephanie (2026-08-03): the field leads, the U.S. team is
       secondary, the board last.

       The full-bleed banner and this section were saying the same thing twice,
       so they are merged (Will, 2026-08-03). The banner's photo and its line
       carry the section; the weaker Karamoja headline is gone. Merging also
       removes the two-photo-bands-butted-together seam the hero created, so
       the divider that patched it is no longer needed. -->
  <RdFeature
    image="/images/hero-pastor-preaching.jpg"
    imageAlt="An indigenous pastor preaching to a gathered crowd under an acacia tree"
    eyebrow="Indigenous leadership"
    heading="National leaders are solving problems others haven't been able to."
    side="right"
    objectPosition="center 35%"
  >
    <p>
      They speak the language, they know the ground, and they were there long before we arrived.
      So the work is not designed in Florida and shipped out. Indigenous leaders decide what their
      own communities need, and we serve that decision.
    </p>
    <p>
      What we build is more of them. Every leader Favor trains is trained to train others: a
      Portable Bible School graduate plants a church, then teaches the next class himself. That is
      how one worker becomes a hundred, and it is why the movement keeps going when we are not
      in the room.
    </p>
  </RdFeature>

  <RdFeature
    image="/images/field-2026/pbs-2.webp"
    imageAlt="A Favor training of trainers session"
    eyebrow="Built to serve the field"
    heading="A small U.S. office, a large field."
    side="left"
  >
    <p>
      Favor employs a large indigenous field team across Uganda, South Sudan, Chad, and 14+
      nations beyond: pastors, teachers, and evangelists serving their own people. Our U.S.
      office stays small on purpose, so the field stays large.
    </p>
  </RdFeature>

  <section class="band band--white">
    <div class="container">
      <LeafHeading
        label="Leadership"
        title="Servant Leaders."
        intro="The role of the U.S. team is to raise prayer and financial support, steward funds with ECFA-grade accountability, and serve the field teams across Uganda, South Sudan, Chad, and 14+ nations beyond."
      />
      <div class="leaders">
        {leadership.map((m, i) => {
          const first = m.fullName.split(' ')[0];
          return (
            <div class="leader reveal" data-side={i % 2 === 0 ? 'left' : 'right'}>
              <div class="leader__media">
                <Leaf class="leader__leaf" />
                <div class="leader__photo"><img src={m.imagePath} alt={m.fullName} width="800" height="800" loading="lazy" /></div>
              </div>
              <div class="leader__copy rd-prose">
                <p class="label">{m.position}</p>
                <h3>{m.fullName}</h3>
                <p class="leader__loc">{m.location}</p>
                <p>{m.bioIntro}</p>
                <p><a href={personHref(m.slug)} class="leader__link">Read {first}'s {m.slug === 'carole-ward' ? 'story' : 'bio'} &rarr;</a></p>
              </div>
            </div>
          );
        })}
      </div>
    </div>
  </section>

  <section class="band band--cream">
    <div class="container">
      <LeafHeading title="Serving the mission from the U.S." leaves={false} />
      <ul class="roster">
        {roster.map((p) => (
          <li class="roster__cell reveal">
            <strong class="roster__name">{p.name}</strong>
            <span class="roster__title">{p.title}</span>
          </li>
        ))}
      </ul>
    </div>
  </section>

  <!-- The overhead split belongs here too, not only on the financials page:
       this is where people are counting U.S. staff. -->
  <section class="band band--white">
    <div class="container container--narrow">
      <LeafHeading
        label="What it costs to run this"
        title="Nine of every ten dollars goes to the field."
        intro="A small U.S. team is how the ratio stays this way. FY2024 audited figures, filed with ECFA and on IRS Form 990."
        leaves={false}
      />
      <ExpenseSplit variant="plain" />
      <p class="band__cta-row">
        <a class="btn btn--secondary" href="/about/accountability/">See our financials</a>
      </p>
    </div>
  </section>

  <RdCta
    heading="See the board."
    body="A volunteer Board of Directors provides governance, financial oversight, and accountability."
    ctaLabel="Meet the Board"
    ctaHref="/about/board/"
    image="/images/field/country-uganda.webp"
    imageAlt="A vision-trip team with Karamojong villagers in the savanna"
  />
</RedesignLayout>

<style>
  /* Leadership as alternating photo + text rows (square photos so faces are
     never cropped), each with a soft leaf behind the photo. */
  /* The decorative leaves sit deliberately outside the photo, which pushed
     36px past the right edge and gave the whole page a sideways scroll on a
     phone. `clip` trims them without creating a scroll container. */
  .leaders { display: flex; flex-direction: column; gap: clamp(56px, 7vw, 112px); margin-top: clamp(40px, 5vw, 72px); overflow-x: clip; }
  .leader { display: grid; grid-template-columns: 0.82fr 1fr; gap: clamp(36px, 5vw, 84px); align-items: center; }
  .leader[data-side='right'] .leader__media { order: 2; }
  .leader[data-side='right'] .leader__copy { order: 1; }
  .leader__media { position: relative; }
  .leader__photo {
    position: relative; z-index: 1;
    aspect-ratio: 1 / 1; max-width: 440px;
    border-radius: var(--rd-radius); overflow: hidden;
    box-shadow: var(--rd-shadow); background: var(--color-sage-light);
  }
  .leader[data-side='left'] .leader__photo { margin-right: auto; }
  .leader[data-side='right'] .leader__photo { margin-left: auto; }
  .leader__photo img { width: 100%; height: 100%; object-fit: cover; display: block; }
  .leader__media :global(.leader__leaf) { position: absolute; z-index: 0; width: clamp(170px, 38%, 290px); height: auto; color: #c2d6b6; bottom: clamp(-58px, -10%, -38px); pointer-events: none; }
  .leader[data-side='left'] :global(.leader__leaf) { left: clamp(-66px, -12%, -40px); transform: rotate(-16deg); }
  .leader[data-side='right'] :global(.leader__leaf) { right: clamp(-66px, -12%, -40px); transform: rotate(16deg) scaleX(-1); }
  .leader__copy .label { margin-bottom: var(--space-2); }
  .leader__copy h3 { color: var(--color-green); font-size: var(--rd-fs-title); margin: 0 0 var(--space-1); }
  .leader__loc { color: var(--color-gold-deep); font-weight: 700; font-size: var(--rd-fs-small); letter-spacing: 0.06em; text-transform: uppercase; margin: 0 0 var(--space-4); }
  .leader__link { color: var(--color-green); font-weight: 700; text-decoration: none; border-bottom: 2px solid var(--color-gold); }
  .leader__link:hover { color: var(--color-gold-deep); }
  @media (max-width: 860px) {
    .leader { grid-template-columns: 1fr; gap: var(--space-8); }
    .leader[data-side='right'] .leader__media { order: 0; }
    .leader[data-side='right'] .leader__copy { order: 0; }
    .leader__photo { max-width: 380px; margin-inline: auto !important; }
  }

  /* Full U.S. roster: a quiet masthead-style directory. Text cells with a
     hairline top rule (no cards; 25 shadowed boxes would be heavy). */
  .roster {
    list-style: none; padding: 0;
    margin: clamp(36px, 4.4vw, 58px) 0 0;
    display: grid; grid-template-columns: repeat(3, 1fr);
    gap: clamp(26px, 3vw, 40px) clamp(32px, 4vw, 56px);
  }
  .roster__cell {
    display: flex; flex-direction: column; gap: var(--space-1);
    padding-top: var(--space-4);
    border-top: 1px solid rgba(31, 58, 26, 0.14);
  }
  .roster__name { color: var(--color-ink); font-weight: 700; font-size: var(--rd-fs-body); }
  .roster__title { color: var(--color-sage); font-size: var(--rd-fs-small); font-weight: 600; letter-spacing: 0.02em; }
  @media (max-width: 900px) { .roster { grid-template-columns: repeat(2, 1fr); } }
  @media (max-width: 520px) { .roster { grid-template-columns: 1fr; } }
</style>
