---
import RedesignLayout from '../../layouts/RedesignLayout.astro';
import PageHero from '../../components/band/PageHero.astro';
import RdFeature from '../../components/redesign/RdFeature.astro';
import LeafHeading from '../../components/redesign/LeafHeading.astro';
import VideoFeature from '../../components/redesign/VideoFeature.astro';
import RdCta from '../../components/redesign/RdCta.astro';

// Team decision, 2026-08: public field travel is project-based. Visiting
// teams cost the field staff time, vehicles, and money, and pull national
// missionaries away from ministry, so Favor no longer advertises ordinary
// short-term trips. The public model is a concrete project (around $20,000,
// e.g. a rural school or a Village Learning Center building) that the
// traveler funds and comes to see through. The URL and nav label stay
// "vision trips"; the content is project participation.
//
// Fact-audit rules still apply (docs/06-reference/about-fact-audit-2026-07-08.md):
// no invented calendars, itineraries, or safety protocols. The $20,000 figure
// and the project examples come from the 2026-08 team decision. Everything
// else is documented program material or an honest "worked out with you"
// process answer.

// Example projects. Program claims stay inside Favor's published material
// (education pillar: primary schools and Village Learning Centers).
const projects = [
  {
    name: 'A rural primary school',
    image: '/images/field-2026/education-5.webp',
    alt: 'A Favor school under construction',
    body: 'Classrooms for a village whose children would otherwise never see one, with the Word of God at the center of the teaching. You fund the build and come for the opening.',
  },
  {
    name: 'A Village Learning Center building',
    image: '/images/field-2026/vlc-3.webp',
    alt: 'A Village Learning Center class in session',
    body: 'A permanent home for a Village Learning Center, where literacy, discipleship, and practical training gather a village week after week. You fund the building and stand in it.',
  },
];

// Films from Favor's YouTube channel (titles match the channel).
const films = [
  { id: '25f290un21o', title: 'They Once Met Under Trees', eyebrow: 'Church planting', heading: 'They once met under trees. Now they have a church.', body: 'The kind of congregation your project serves: villages where the church was planted by leaders who grew up there.' },
  { id: '4YpOvDoc6k4', title: 'Where Medicine Meets Miracles', eyebrow: 'Medical work', heading: 'Inside Favor’s medical center in Africa.', body: 'The daily work your project joins on the ground, run by indigenous leaders year-round.' },
];

// Who project trips are for. Deliberately a filter: serious inquirers should
// feel invited, and mission-trip shoppers should keep scrolling.
const audiences = [
  { icon: 'church', title: 'Churches ready to build', body: 'A congregation that wants to fund a school or a Learning Center building together and send a few of its own to see it through.' },
  { icon: 'pin', title: 'Partners with skin in the game', body: 'Givers who want their next gift to be one concrete thing, with a name and a place, that they stand inside when it opens.' },
  { icon: 'compass', title: 'Leaders who finish things', body: 'People who can rally others around a $20,000 goal and stay with the project until the doors open.' },
];
const WHO_ICONS: Record<string, string> = {
  church: '<path d="M12 3v4M10 5h4"/><path d="M4 21v-8l8-5 8 5v8"/><path d="M10 21v-5a2 2 0 0 1 4 0v5"/><path d="M3 21h18"/>',
  pin: '<path d="M12 21s-6.2-5.4-6.2-10.2a6.2 6.2 0 1 1 12.4 0C18.2 15.6 12 21 12 21Z"/><circle cx="12" cy="10.6" r="2.4"/>',
  compass: '<circle cx="12" cy="12" r="9"/><path d="M15.5 8.5l-1.8 5.2-5.2 1.8 1.8-5.2 5.2-1.8Z"/>',
};

const steps = [
  { n: '1', title: 'Inquire', body: 'Write to the US team through the contact form. Tell us who you are, who would come with you, and what kind of project you could carry.' },
  { n: '2', title: 'Adopt and fund a project', body: 'The field names its current needs. You take one on and raise the funds for it, around $20,000, with your church, your group, or on your own. The US team walks the fundraising with you.' },
  { n: '3', title: 'Go see it through', body: 'You travel in a small team, hosted by the field leaders who run the work, and stand in the project you funded.' },
];

// FAQ: honest process answers. The only figure is the $20,000 project model;
// every other specific routes to the planning conversation.
const faqs = [
  { q: 'What does it cost?', a: 'The model is a project of around $20,000, plus your own travel costs. The exact figure depends on the project the field names, and the US team will talk through fundraising with you before you commit to anything.' },
  { q: 'Can I visit without a project?', a: 'No. Hosting guests costs the field staff time, vehicles, and money, so every trip is tied to a project. If you want to support the work without traveling, start with the giving pages or serve from home.' },
  { q: 'Do you take high school or college mission teams?', a: 'No. We know that is a hard word for groups with good intentions, and we say it plainly because the field’s time belongs to the ministry. A student group ready to raise funds for a real project is welcome to start that conversation.' },
  { q: 'How long does this take?', a: 'As long as the project takes. Fundraising runs at your pace, the build runs on the field’s timeline, and travel is planned around a moment worth being there for. The US team works the specifics out with you.' },
  { q: 'Who plans the travel?', a: 'The US team plans it with you, and the field leaders host you on the ground. Timing, length, destination, lodging, documents, and health requirements are all worked out before you commit, and we are honest about current conditions and what we can and cannot promise.' },
  { q: 'How do I start?', a: 'Use the contact form. Tell us who you are, who would travel, and what kind of project you could carry.' },
];
---
<RedesignLayout
  title="Vision Trips"
  description="Favor's field travel is project-based. Fund a concrete project, around $20,000 for a rural school or a Village Learning Center building, and come see it through with the leaders who run the work."
  image="/images/field/hero-jesusfilm-chad.webp"
>
  <PageHero
    label="Project trips"
    title="Fund a project. Come see it through."
    promise="Field travel with Favor is tied to a concrete project: around $20,000 for a rural school or a Village Learning Center building, funded by you and finished with the leaders who run the work."
    image="/videos/vision-trips-hero-poster.jpg"
    imageAlt="A village congregation gathered for worship, from the film They Once Met Under Trees"
    video="/videos/vision-trips-hero.mp4"
    ctaLabel="Start a project inquiry"
    ctaHref="/about/contact/?topic=vision-trip"
    scrollCue
  />

  <RdFeature
    image="/images/story-vision-trip-team.jpg"
    imageAlt="A vision-trip team with villagers in the field"
    eyebrow="How going works"
    heading="You carry a project. The field hosts you."
    side="right"
  >
    <p>
      A trip with Favor starts long before the flight. You adopt a specific project the
      field has named, raise the funds for it, and then travel with a small team to see it
      through on the ground with the indigenous leaders who will run it.
    </p>
    <p>
      When you fly home, the school stays open. The work your project joins runs every day
      across Uganda, South Sudan, and Chad. See
      <a class="story-link" href="/about/where-we-work/">where we work</a> for what it looks
      like in each nation.
    </p>
  </RdFeature>

  <section class="band band--cream">
    <div class="container container--narrow">
      <LeafHeading
        label="Why project-based"
        title="A visiting team costs the field something."
        leaves={false}
      />
      <div class="why rd-prose">
        <p>
          Hosting a team takes field staff time, vehicles, fuel, and money, and it pulls
          national missionaries away from the ministry they were sent to do. We will not
          spend the field&rsquo;s attention on tourism, and we do not plan trips for travelers
          who want to experience Africa or add a mission trip to a testimony.
        </p>
        <p>
          So every trip is tied to a project. Your fundraising pays for something the field
          has asked for, and your visit marks its completion instead of interrupting the
          work. This asks more of you than a typical short-term trip, and that is deliberate.
          If you want to build something that outlasts your visit, we want to hear from you.
        </p>
      </div>
    </div>
  </section>

  <section class="band band--white">
    <div class="container">
      <LeafHeading
        label="The projects"
        title="Projects you can carry."
        intro="Around $20,000 funds a project like these. The field team names the current needs, so the first step is a conversation."
        leaves={false}
      />
      <div class="projects">
        {projects.map((p, i) => (
          <div class="projects__row reveal" data-side={i % 2 === 0 ? 'left' : 'right'}>
            <figure class="projects__media">
              <img src={p.image} alt={p.alt} loading="lazy" width="1200" height="800" />
            </figure>
            <div class="projects__copy rd-prose">
              <h3>{p.name}</h3>
              <p>{p.body}</p>
            </div>
          </div>
        ))}
      </div>
      <p class="projects__note">
        Want to fund a project without traveling? Start with
        <a class="story-link" href="/give/specific-project/">specific-project giving</a>.
      </p>
    </div>
  </section>

  <section class="band band--cream">
    <div class="container">
      <LeafHeading label="How it works" title="Three steps, no fine print." leaves={false} />
      <ol class="steps">
        {steps.map((s) => (
          <li class="rd-card steps__item reveal">
            <span class="steps__n" aria-hidden="true">{s.n}</span>
            <h3>{s.title}</h3>
            <p>{s.body}</p>
          </li>
        ))}
      </ol>
    </div>
  </section>

  <section class="band band--white">
    <div class="container">
      <LeafHeading label="Is this you?" title="Who project trips are for." leaves={false} />
      <div class="who">
        {audiences.map((a) => (
          <div class="who__col reveal">
            <span class="who__icon" aria-hidden="true">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" set:html={WHO_ICONS[a.icon]}></svg>
            </span>
            <h3>{a.title}</h3>
            <p>{a.body}</p>
          </div>
        ))}
      </div>
      <p class="who__plain">
        Favor does not host high school or college mission-trip teams, and we do not plan
        travel for visitors who want to see the work without carrying part of it. Every
        guest the field hosts costs staff time, vehicles, and money, and the work comes first.
      </p>
    </div>
  </section>

  <section class="band band--cream">
    <div class="container">
      <LeafHeading label="From the field" title="The work your project joins." />
      <div class="vrows">
        {films.map((f, i) => (
          <VideoFeature id={f.id} videoTitle={f.title} eyebrow={f.eyebrow} heading={f.heading} side={i % 2 === 0 ? 'left' : 'right'} decor="leaf">
            <p>{f.body}</p>
          </VideoFeature>
        ))}
      </div>
    </div>
  </section>

  <section class="band band--white">
    <div class="container container--narrow">
      <LeafHeading label="Questions, answered" title="Before you ask." leaves={false} />
      <div class="faq">
        {faqs.map((f) => (
          <details class="faq__item rd-outline">
            <summary>{f.q}</summary>
            <p>{f.a} {f.q === 'How do I start?' && <a class="story-link" href="/about/contact/?topic=vision-trip">Start here</a>}</p>
          </details>
        ))}
      </div>
    </div>
  </section>

  <RdCta
    heading="Bring us a project inquiry."
    body="Tell us who you are, who would come with you, and what you could raise. You will get a straight answer and a real project."
    ctaLabel="Start the conversation"
    ctaHref="/about/contact/?topic=vision-trip"
    background="cream"
    image="/images/field-2026/gift-4.webp"
    imageAlt="A Favor gathering in the field"
  />
</RedesignLayout>

<style>
  /* Why project-based: narrow prose band. */
  .why { margin-top: clamp(28px, 3.5vw, 44px); }
  .why p { color: var(--color-ink-soft); line-height: 1.75; }

  /* Example projects: alternating photo + text rows. */
  .projects { display: flex; flex-direction: column; gap: clamp(48px, 6vw, 88px); margin-top: clamp(40px, 5vw, 64px); }
  .projects__row { display: grid; grid-template-columns: 1.15fr 1fr; gap: clamp(36px, 5vw, 84px); align-items: center; }
  .projects__row[data-side='right'] .projects__media { order: 2; }
  .projects__row[data-side='right'] .projects__copy { order: 1; }
  .projects__media { margin: 0; }
  .projects__media img {
    display: block; width: 100%; height: auto; aspect-ratio: 3 / 2; object-fit: cover;
    border-radius: var(--rd-radius); box-shadow: var(--rd-shadow);
  }
  .projects__copy h3 { color: var(--color-green); margin: 0 0 var(--space-3); }
  .projects__copy p { color: var(--color-ink-soft); }
  .projects__note { text-align: center; color: var(--color-ink-soft); margin: clamp(40px, 5vw, 64px) 0 0; }
  @media (max-width: 860px) {
    .projects__row { grid-template-columns: 1fr; gap: var(--space-6); }
    .projects__row[data-side='right'] .projects__media { order: 0; }
    .projects__row[data-side='right'] .projects__copy { order: 0; }
  }

  /* Video rows share the WWW spacing. */
  .vrows { display: flex; flex-direction: column; gap: clamp(56px, 7vw, 100px); margin-top: clamp(40px, 5vw, 64px); }

  /* Who it's for: open columns with gold icon medallions and hairline
     separators. Deliberately NOT boxed cards, so it reads nothing like the
     numbered "How it works" cards above. */
  .who { display: grid; grid-template-columns: repeat(3, 1fr); margin-top: clamp(36px, 4.4vw, 58px); }
  .who__col { padding: var(--space-2) clamp(24px, 3vw, 48px); text-align: center; }
  .who__col + .who__col { border-left: 1px solid rgba(31, 58, 26, 0.14); }
  .who__icon {
    display: inline-flex; align-items: center; justify-content: center;
    width: 64px; height: 64px; border-radius: 50%;
    background: color-mix(in srgb, var(--color-gold) 16%, var(--color-white));
    color: var(--color-gold-deep);
    margin-bottom: var(--space-5);
  }
  .who__icon svg { width: 30px; height: 30px; }
  .who__col h3 { color: var(--color-green); margin: 0 0 var(--space-3); }
  .who__col p { margin: 0 auto; color: var(--color-ink-soft); line-height: 1.7; max-width: 34ch; }
  .who__plain {
    margin: clamp(40px, 5vw, 60px) auto 0; max-width: 62ch; text-align: center;
    color: var(--color-ink); font-weight: 600; line-height: 1.7;
  }
  @media (max-width: 860px) {
    .who { grid-template-columns: 1fr; gap: var(--space-8); }
    .who__col { padding: 0; }
    .who__col + .who__col { border-left: 0; border-top: 1px solid rgba(31, 58, 26, 0.14); padding-top: var(--space-8); }
  }

  /* Steps. */
  .steps { list-style: none; padding: 0; margin: clamp(36px, 4.4vw, 58px) 0 0; display: grid; grid-template-columns: repeat(3, 1fr); gap: clamp(20px, 2.4vw, 32px); }
  @media (max-width: 860px) { .steps { grid-template-columns: 1fr; } }
  .steps__n {
    display: inline-flex; align-items: center; justify-content: center;
    width: 44px; height: 44px; border-radius: 50%;
    background: var(--rd-bg-green); color: var(--color-green);
    font-weight: 800; font-size: 1.15rem; margin-bottom: var(--space-4);
  }
  .steps__item h3 { color: var(--color-green); margin: 0 0 var(--space-3); }
  .steps__item p { margin: 0; color: var(--color-ink-soft); line-height: 1.7; }

  /* FAQ: native accordion, outlined rows. */
  .faq { display: flex; flex-direction: column; gap: var(--space-3); margin-top: clamp(36px, 4.4vw, 58px); }
  .faq__item { padding: 0; overflow: hidden; }
  .faq__item summary {
    cursor: pointer; list-style: none; position: relative;
    padding: var(--space-5) var(--space-12) var(--space-5) var(--space-6);
    font-weight: 700; color: var(--color-ink);
  }
  .faq__item summary::-webkit-details-marker { display: none; }
  .faq__item summary::after {
    content: '+'; position: absolute; right: var(--space-6); top: 50%; transform: translateY(-50%);
    color: var(--color-green); font-weight: 700; font-size: 1.3rem; line-height: 1;
    transition: transform 160ms ease;
  }
  .faq__item[open] summary::after { transform: translateY(-50%) rotate(45deg); }
  .faq__item summary:hover { color: var(--color-green); }
  .faq__item p { margin: 0; padding: 0 var(--space-6) var(--space-5); color: var(--color-ink-soft); line-height: 1.7; }
</style>
