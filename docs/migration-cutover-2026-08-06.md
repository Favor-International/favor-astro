# Cutover audit and redirect map (2026-08-06)

Everything done before pointing favorintl.org at the new site.

## What exists on the old site

The old Webflow sitemap holds **638 URLs**, all crawled and archived today
(every page returned 200):

| Section | URLs | Where they go now |
|---|---|---|
| /newsletters/* (old newsletter posts) | 306 | /newsletter/archive/ |
| /testimonies-updates/* (stories, reports, media) | 136 | exact story page where the slug matches (115 do), reports to /about/accountability/, rest to /stories/ |
| tag taxonomies (8 different trees) | 135 | /stories/ (newsletter tags to /newsletter/archive/) |
| /what-we-do/* (programs) | 20 | /impact/... (pbs→portable-bible-schools, gift→gift-institute, planting→church-planting, prayer→/pray/) |
| /about/*, /directors/* | 17 | /about/... (ecfa-accountability→accountability, directors→board, people pages matched by slug) |
| /connect-support/*, /positions/*, /current-projects/* | 13 | /give/..., /go/careers/, /give/specific-project/ |
| leftovers (carts, intro-flyer, thanksgiving...) | 11 | /books/ for carts; junk pages to / |

Artifacts:
- `docs/06-reference/old-site-inventory-2026-08-06.json`: every old URL with
  status, title, meta description, and H1.
- `docs/06-reference/ranked-keywords-2026-08-06.json`: every keyword the old
  domain ranks for (DataForSEO Labs, US).
- Full HTML archive of all 638 pages (24MB raw, 5.7MB zipped), kept out of the
  repo at `C:\Users\Willb\Claude\favor-old-site-archive-2026-08-06.zip`.

## What the old site ranks for, and whether we kept it

DataForSEO says the domain ranks for **43 keywords total** in the US. Every
ranked URL has a topical equivalent on the new site and an explicit 301 to it.
The ones that matter:

| Ranking page | Best terms | 301 target | Verdict |
|---|---|---|---|
| / | favor international #1, favor ministries #2 | / | kept |
| /directors/carole-ward | **carole ward missionary #1, carole ward #2** | /about/our-story/founder/ | kept, stronger page |
| /about/our-story/founder | carole ward husband #3 | same path exists | kept |
| /connect-support/donate-now | give favor #7 | /give/donate/ | kept |
| /testimonies-updates/.../wars-that-win-souls | god help sudan (v1000) | /stories/wars-that-win-souls/ | kept, exact story |
| /what-we-do/.../radio | what is a christian radio station | /impact/.../radio/ | kept |
| /what-we-do/.../construction | grants for church building projects | /impact/.../construction/ | kept |
| /what-we-do/.../prayer | house of prayer international | /pray/ | kept |
| /what-we-do/economic-empowerment/women | women's empowerment international | /impact/.../women/ | kept |
| /about/directors + /directors/stephanie-maier | (name queries) | /about/board/ + /about/people/stephanie-maier/ | kept |
| /what-we-do/education/centers | village schools international #17 | /impact/education/centers/ | kept |
| /positions/country-director | country director #16 | /go/careers/ (posting lives there) | kept |

**No ranking term is orphaned.** The genuine content loss is long-tail: the
306 individual newsletter posts and ~21 unmatched story pages collapse into
their index pages. None of them rank for anything today, so nothing measurable
is lost; if any had quiet traffic, the 301s hand it to the right section.

## The redirect machinery

Two layers, deliberately:

1. **`public/_redirects`** (generated by `scripts/build-redirects.mjs`,
   656 rules): an explicit 301 for every old sitemap URL, `/sitemap.xml` →
   `/sitemap-index.xml` for Search Console, then structural wildcards for
   each old URL tree. (www→apex is NOT here: Pages rejects absolute-URL
   sources, so it is a zone Redirect Rule, step 3 below.) Regenerate any time with
   `node scripts/build-redirects.mjs` (needs `~/old-urls.txt`).
2. **`functions/_middleware.ts`**: the true catch-all. Any URL nobody ever
   knew about 404s and becomes a 301 to the homepage. Narrow on purpose:
   GET/HEAD only, HTML navigations only (a missing image stays 404), never
   /api/*. A bare `/*` rule in _redirects would run BEFORE static assets and
   take the whole site down, which is why the catch-all is code.

Verified locally against a served build: 10 representative redirects all land
on their exact targets (including the carole-ward and wars-that-win-souls
pages), real pages still 200, the API untouched, unknown URLs 301 home, and a
missing image stays a 404.

## Cutover: DONE 2026-08-06

Executed via the API the same day:
1. Custom domains favorintl.org and www.favorintl.org attached to the
   favor-astro Pages project. Apex went active; the www attachment can sit
   "pending" forever because the zone redirect intercepts www at the edge
   before Pages ever sees it (the zone universal cert covers www TLS).
2. DNS flipped, exactly two records touched in a 72-record zone:
   apex A 198.202.211.1 -> CNAME favor-astro.pages.dev (proxied), and
   www CNAME cdn.webflow.com -> favor-astro.pages.dev (proxied). Mail (MX),
   the flipbook subdomains, contact, my.favorintl.org, and every other
   subdomain untouched and re-verified 200 after the flip.
3. Zone Redirect Rule created (http_request_dynamic_redirect): host
   www.favorintl.org -> 301 https://favorintl.org + path, query preserved.

Verified live on the real domain after cutover: homepage 200 with the new
hero, www folds to apex (301 -> 200), legacy URLs 301 to exact targets
(donate-now, carole-ward, pbs), unknown URLs 301 home, giving connected in
Live mode with 5 designations.

Still for a human: submit /sitemap-index.xml in Google Search Console (needs
GSC login), and eyeball the ranked URLs from the table over the next weeks.
